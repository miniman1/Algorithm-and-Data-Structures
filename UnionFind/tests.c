#include "UnionFind.h"

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
    // invalid size
        // invalid find
        // valid find

        // invalid union
        // valid union 

    // size == 1
        // invalid find
        // valid find

        // invalid union
        // valid union 

    // size > 1 (e.g. 4)
        // invalid find
        // valid find

        // invalid union
        // valid union 
}

